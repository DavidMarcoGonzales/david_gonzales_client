// import { SET_CURRENT_CARD, SET_CARD_SUCCESS } from "../actions/actionTypes";
import initialState from './initialState';

export default function vid(state = initialState.explainations, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
